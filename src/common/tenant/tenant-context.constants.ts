// src/common/tenant/tenant-context.constants.ts

/** CLS key holding the active coProperty id (string) for the current request. */
export const ACTIVE_COPROPERTY_KEY = 'activeCoProperty';

/** Request header carrying the coproperty the caller wants to act on. */
export const COPROPERTY_HEADER = 'x-coproperty-id';
